# Scheduled tests for serlo.org

A github workflow to periodically tests if serlo.org is online and if core functionality (like login) is working.

Uses cypress to run tests.

Sends email when tests fail.
